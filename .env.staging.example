# Brezel.ai Staging Environment Configuration
# Copy this file to .env.staging and customize for your setup

# Database Configuration
# For external PostgreSQL server (Linux server IP or hostname)
DSN=postgres://username:password@your-postgres-host:5432/database_name?sslmode=disable

# For Docker Desktop on macOS/Windows (connects to host PostgreSQL)
# DSN=postgres://username:password@host.docker.internal:5432/database_name?sslmode=disable

# For Linux Docker (connects to host PostgreSQL)
# DSN=postgres://username:password@172.17.0.1:5432/database_name?sslmode=disable

# Application Configuration
WEB_ADDR=:8080
DATA_FOLDER=/gmapsdata
LOG_LEVEL=info

# Optional: Authentication (if using Clerk)
CLERK_API_KEY=

# Optional: Telemetry (set to 1 to disable)
DISABLE_TELEMETRY=0